/** AceEndico uses the shared history → suggestion pipeline (same thresholds as all vendors). */
export { generateSuggestedOrderItemsFromHistory as generateAceEndicoSuggestedOrder } from '../shared/vendorData/suggestOrderFromHistory'
